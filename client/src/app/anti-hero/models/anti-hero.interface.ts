export interface AntiHero {
  id: string;
  firstName: string;
  lastName: string;
  house: string;
  knownAs: string;
}

export enum TableActions {

}


export enum CommandBarActions {
  Create,
  DeleteAll
}
